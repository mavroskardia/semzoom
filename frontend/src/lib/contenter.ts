import { SemzoomContent } from "../types/content";

export class SemzoomContenter {

  make_content(data: SemzoomContent) {
    // data.content is a DSL that describes an infinitely zoomable canvas...somehow

  }
}
